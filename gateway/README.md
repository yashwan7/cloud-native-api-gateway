# Gateway

The gateway is a dependency-free Node.js HTTP reverse proxy. Routes are configured through service URL environment variables; Docker Compose and Kubernetes provide the correct values for each environment.
